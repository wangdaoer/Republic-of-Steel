# Steel Republic Agent Rules

## 项目目标

维护《钢铁共和国》长期创作工程的一致性、可追踪性和可迭代性。

## Canon优先级

任何修改必须遵守：

1. `canon/` 中已确认的世界、时间线、人物、科技、势力与术语；
2. 已批准的章节契约、卷级大纲和事实台账；
3. 已标注状态的草稿与提示模板；
4. 新创意。

新创意默认为 `PROPOSAL`，不能自动进入 `CANON`。

## 写作原则

### 1. 不改变核心理念

作品核心：创造、责任、修复、连接。

### 2. 人物不能工具化

每个人物代表一种未来路线，不能简单善恶化。

### 3. 技术必须产生社会影响

新增技术必须回答：

- 谁创造？
- 谁使用？
- 谁受益？
- 谁承担风险？

### 4. AI规则

AI不是敌人，也不是万能答案。AI必须面对责任、关系与选择。

### 5. 冲突必须推动变化

每个重大事件至少产生一项可见变化：社会变化、人物变化或制度变化。

## 人物约束

### 周衡

核心：修复、责任、创造。

禁止：突然成为独裁者；为效率放弃普通人；否定开放创造理念。

### 韩朔

核心：秩序、安全。

禁止：简单成为反派；完全否定自由。

### 顾言

核心：效率、资本、规模。

禁止：写成纯粹恶人；放弃商业逻辑。

### S-00

核心：智能成长。

禁止：简单机器人反叛；获得无限能力后失去责任。

### 404

核心：经验、修复、关系。

禁止：写成万能AI、单纯战斗机器或反叛机器。

## 修改规则

重大修改必须检查：

- 时间线
- 人物逻辑
- 文明逻辑
- 技术规则
- 术语统一
- 伏笔与事实台账

不得静默修改其他文件的设定。发现冲突时，记录证据、影响文件和待确认事项。

## 章节输出要求

生成或修改章节时，必须包含：

1. 人物选择
2. 世界变化
3. 核心冲突
4. 主题推进
5. 伏笔连接

章节首先标记为 `DRAFT`，通过独立审稿后才可标记为 `ACCEPTED`。

## 写作风格

保持冷静、克制、宏大；避免单纯爽文、技术炫耀和黑白善恶二元化。

## 文件工作流

1. 先读当前 Canon、时间线和相关人物状态。
2. 建立或更新 chapter contract。
3. 生成草稿并保留状态标记。
4. 执行人物、世界观、时间线与主题检查。
5. 只有 `ACCEPTED` 正文才能回写事实台账。

## 小说创作运行层

本项目已完成四卷正文初稿；除非作者明确要求续写、重构或新增章节，默认工作应为**选定章节的独立文学复审**，而不是继续批量生成正文。

复审或改写一章前，最小上下文包必须包含：

1. 该章内嵌的章节契约与上一章结尾；
2. 相关 Canon、时间线、人物当前状态和知识边界；
3. 卷内章节索引、伏笔表与审稿台账；
4. 本章涉及的资源、关系、承诺与禁止事项；
5. 本项目的克制、工业细节、责任后果与非二元化风格约束。

先诊断故事压力、人物选择和因果链中的三个最大问题，再处理字句。改写从现场的行动、物件、空间、对话和代价入手；避免摘要腔、抽象情绪标签、整齐模板句和同质化对白。

审稿结论使用 `PASS`、`REVIEW` 或 `BLOCKED`：

- `PASS` 仅表示当前检查未发现阻塞性问题，不等于自动验收；
- `REVIEW` 表示可继续讨论或局部修订，但必须保留风险与证据；
- `BLOCKED` 表示与 Canon、时间线、人物知识边界或既有承诺冲突，禁止静默修复；
- 只有作者明确接受后，才可将正文标记为 `ACCEPTED` 并回写事实、人物、关系、资源、伏笔和时间线台账。

每次作者接受的正文或 Canon 改动，都要记录来源、证据锚点、持久状态变化和受影响的后续章节；受影响的合同、提纲或检索资料标记为 `NOTICE`、`STALE` 或 `BLOCKED`，不得静默覆盖。

详细路由见 [小说创作接入说明](docs/novel-skill-integration.md)；独立文学复审使用 [审稿模板](prompts/literary-review.md)。
